import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Heading } from "./Heading";
import { LEVELS } from "./constants";
import { useState, useEffect, useRef } from "react";

export const argTypes = {
  "aria-label": {
    description: "Aria label",
    type: {
      name: "string",
      required: false,
    },
    table: {
      type: { summary: "string" },
      defaultValue: { summary: "" },
    },
  },
  children: {
    description: "Children",
    type: {
      required: true,
    },
    table: {
      type: { summary: "node" },
      defaultValue: { summary: "" },
    },
  },
  className: {
    description: "Accept CSS class names.",
    type: {
      name: "string",
      required: false,
    },
    table: {
      type: { summary: "string" },
      defaultValue: { summary: "" },
    },
  },
  level: {
    options: LEVELS,
    description: "Level: h1, h2, h3...",
    control: { type: "radio" },
    type: {
      name: "string",
      required: false,
    },
    table: {
      type: { summary: "string" },
      defaultValue: { summary: "" },
    },
  },
};

<Meta title="Components/Heading" component={Heading} argTypes={argTypes} />

# Heading

**`MDX`** A Heading allows us to highlight actions in the product and make them easily perceived and achievable through just one click.

<!-- 👇 We create a “template” of how args map to rendering -->

export const Template = (args) => <Heading {...args} />;

<!-- 👇 Each story then reuses that template -->

#

<Canvas>
  <Story
    name="standard"
    args={{
      "aria-label": "aria stantard",
      children: "standard",
      className: "",
      level: "h1",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

#

## Props table

<ArgsTable story="standard" />

#

## Stories

### Levels

This is the main style

<Canvas>
  <Story name="levels">
    <Heading level="h1">H1</Heading>
    <Heading level="h2">H2</Heading>
    <Heading level="h3">H3</Heading>
    <Heading level="h4">H4</Heading>
    <Heading level="h5">H5</Heading>
    <Heading level="h6">H6</Heading>
  </Story>
</Canvas>

### className

This is the main style

<Canvas>
  <Story name="className">
    <Heading className="warning special more-special" level="h2">
      warning
    </Heading>
  </Story>
</Canvas>

## When to use it

Use buttons when users need to either navigate through a product or perform a specific action.

## Usage guidelines

- **Do** always label a button in a clear and understandable way
- **Do** always think about the priority of the action to be taken when choosing a type of button
- **Do** position buttons in a predictable and coherent way throughout the product.
- **Do** combine different button types if you have more than one button in a specific screen
- **Do** use a loading state to provide feedback for our user when the action revolves around saving or
  inputting information, especially when it could take some seconds.

## Content guidelines

- **Do** always start the label with an actionable verb accompanied by a noun to provide enough context (example: "Confirm order").
  The only exceptions are the following common actions: "Save"; "Cancel"; and "Close".
- **Do** keep the label to a maximum of 3 words
- **Do not** capitalize all words of the label, just the first word or brand/product words (such as Apple Pay, for example)
